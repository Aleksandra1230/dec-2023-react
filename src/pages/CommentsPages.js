import React from 'react';
import {Comments} from "../components/Comments";

const CommentsPages = () => {
    return (
        <div>
            <Comments/>
        </div>
    );
};

export default CommentsPages;