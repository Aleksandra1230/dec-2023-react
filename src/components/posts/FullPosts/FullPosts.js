import React from 'react';

const FullPosts = (value) => {
    return (
        <div>
            {
                JSON.stringify(value)
            }
            
        </div>
    );
};

export default FullPosts;