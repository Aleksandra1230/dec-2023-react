import React from 'react';

const User = (value) => {
    return (
        <div>
            <div>id: {value.id}</div>
            <div>name: {value.name}</div>

        </div>
    );
};

export default User;